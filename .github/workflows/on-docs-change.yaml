name: Pull Markdown Files

on:
  repository_dispatch:
    types: [docs_changed]

jobs:
  pullMarkdown:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout Package Repository
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.client_payload.repository }}
          ref: master
          path: './temp'

      - name: Copy Files
        run: |
          mkdir -p "./docs/${{ github.event.client_payload.name }}"
          cp "./temp/README.md" "./docs/${{ github.event.client_payload.name }}"
          find ./temp/docs -name "*.md" -exec cp {} "./docs/${{ github.event.client_payload.name }}" \;

      - name: Remove Temp Directory
        run: rm -rf "./temp"

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.DOCS_PAT }}
          commit-message: "Merge new docs from ${{ github.event.client_payload.repository }}"
          branch: "${{ github.event.client_payload.repository }}-${{ github.run_id }}"
          delete-branch: true
          title: "Update docs from ${{ github.event.client_payload.repository }}"
          body: "This PR merges documentation updates from ${{ github.event.client_payload.repository }}"
          labels: "documentation, automated pr"
          draft: false
